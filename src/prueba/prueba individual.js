//Ejercicio- obten el promedio de los estudiantes
function promedio(listaObje) {
    var stotal =+ number(totaldeci.toFixed(2)); //variable convertida en numero con exactitud de 2 decimales
    var stotalprome = stotal/i;
    for (let i = 0; i <= listaObje.length; i++) { //itero los elementos de la lista de objetos
        var prome = listaObje[i]; //accedo a cada objeto
        var sumanota = prome[grades]; //accedo a la propiedad
        var total = sumaTnota / j; //calculo el promedio individual
        var totaldeci = number(total.toFixed(2)); //coercion a number + exactitud 2 decimales 
        for (let j = 0; j <= sumanota.length; j++) { //itero el array de la propiedad grades
            var sumaTnota =+ sumanota[j]; //acumulo la suma de notas
            
        }
    }
    console.log({
        classAverage: stotal, 
        students:[]})

}